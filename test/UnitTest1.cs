namespace test;

public class UnitTest1
{
    [Fact]
    public void SingleNodeCluster_WhenNodeTransitionToCandidate_NodeVotesForItself()
    {
        // Given a single node cluster, when the node transitions to “candidate” state, then the node votes for itself.
    }
}